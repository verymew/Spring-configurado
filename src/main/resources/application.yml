spring:
  datasource:
    username: postgres
    password: ju
    url: jdbc:postgresql://localhost:5432/evianda
  jpa:
    hibernate:
      ddl-auto: update

jwt:
  private:
    key: classpath:app.key
  public:
    key: classpath:app.pub

